{
  "name": "clean-architecture",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "start": "tsc && node ./dist/index.js",
    "dev": "tsnd --respawn --ignore-watch node_modules --transpile-only --no-notify src/index.ts",
    "test": "tsc && jest ./dist"
  },
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "ts-jest": "^26.5.4",
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "jest": "^26.6.3"
  }
}
